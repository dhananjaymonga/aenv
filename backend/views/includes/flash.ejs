<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (success && success.length) { %>
        <div class="bg-green-500 text-white p-3 rounded-lg text-center mt-6 mx-auto max-w-md">
          <%= success %>
        </div>
      <% } %>
      
      <% if (error && error.length) { %>
        <div class="bg-red-500 text-white p-3 rounded-lg text-center mt-6 mx-auto max-w-md">
          <%= error %>
        </div>
      <% } %>
      
</body>
</html>