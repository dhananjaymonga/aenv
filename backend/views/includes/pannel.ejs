<div class=" rounded-lg p-4 mx-6 mt-4">
    <form action="/listings" method="GET" class="grid grid-cols-1 md:grid-cols-4 gap-4 items-center">
        
        <!-- Search Bar -->
        <div class="relative">
            <input type="text" name="search" placeholder="Search locations..." class="w-full border rounded-lg py-2 px-4">
            <span class="absolute right-3 top-2 text-gray-500">🔍</span>
        </div>

        <!-- Price Range -->
        <div class="flex items-center space-x-2">
            <span class="text-gray-600">💰</span>
            <input type="number" name="minPrice" placeholder="Min" class="w-20 border rounded-lg py-2 px-3">
            <input type="number" name="maxPrice" placeholder="Max" class="w-20 border rounded-lg py-2 px-3">
        </div>

        <!-- Property Type -->
        <div>
            <select name="type" class="w-full border rounded-lg py-2 px-4">
                <option value="">🏠 Property Type</option>
                <option value="apartment">Apartment</option>
                <option value="house">House</option>
                <option value="villa">Villa</option>
                <option value="cabin">Cabin</option>
            </select>
        </div>

        <!-- Amenities -->
        <div class="flex items-center space-x-2">
            <label class="flex items-center space-x-1">
                <input type="checkbox" name="wifi" value="true" class="form-checkbox"> 
                <span>📶 WiFi</span>
            </label>
            <label class="flex items-center space-x-1">
                <input type="checkbox" name="parking" value="true" class="form-checkbox"> 
                <span>🚗 Parking</span>
            </label>
            <label class="flex items-center space-x-1">
                <input type="checkbox" name="pool" value="true" class="form-checkbox"> 
                <span>🏊 Pool</span>
            </label>
        </div>

        <!-- Submit Button -->
        <!-- <div class="col-span-1 md:col-span-4 text-center">
            <button type="submit" class="bg-red-500 text-white px-6 py-2 rounded-lg hover:bg-red-600">Search</button>
        </div> -->
    </form>
</div>
